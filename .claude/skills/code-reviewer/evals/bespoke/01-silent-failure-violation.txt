// ===== FILE: src/services/inventoryService.ts =====
export async function getAvailableStock(sku: string): Promise<number> {
  try {
    const row = await inventoryRepository.findBySku(sku);
    return row.quantity;
  } catch (e) {
    return 0;
  }
}
